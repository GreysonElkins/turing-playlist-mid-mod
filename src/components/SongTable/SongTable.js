import React from 'react'

const SongTable = ({ allSongs }) => {

  const galleryWithSongs = allSongs.map(song => {
    // return a movieCard
  })

  return (<>
    </>
  )
}

export default SongTable